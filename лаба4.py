#######             ЛАБА 4 ЗАДАНИЕ 1                    ##########

# ##Отчёт по лабораторной работе №4

####Студент: Danila Litviniuk

#### Цель работы

# Целью лабораторной работы было освоение базовых методов биоинформатического анализа с помощью языка Python и библиотеки Biopython:
# объединение файлов в формате GenBank, расчет GC-содержания нуклеотидных последовательностей, сортировка последовательностей по
# содержанию GC, а также извлечение и аннотирование белковых последовательностей из геномных данных.

#### Задачи

#  1. Научиться объединять несколько GenBank-файлов в один общий файл.
#  2. Реализовать функцию для расчёта процентного содержания GC (гуанин+цитозин) в последовательностях.
#  3. Сортировать и выводить последовательности по содержанию GC.
#  4. Извлекать белковые последовательности и их аннотации из файла GenBank.
#  5. Оформить вывод информации о найденных белках (ID, описание, координаты, аминокислотная последовательность).

####Инструменты и алгоритмы

# Инструменты:

#  • Язык программирования Python 3
#  • Библиотека Biopython (модуль SeqIO)
#  • Формат файлов: GenBank

# Алгоритмы и этапы работы:

# 1. Объединение GenBank-файлов (Задание 1)

#  • С помощью SeqIO.parse открываются два файла в формате GenBank.
#  • Все записи из обоих файлов последовательно записываются в новый файл с помощью SeqIO.write.
#  • После завершения работы выводится сообщение об успешном объединении.
# 2. Расчёт GC-содержания (Задание 2)

#  • Функция calculate_gc_content(sequence) подсчитывает количество символов ‘G’ и ‘C’ в последовательности.
#  • Результат выражается в процентах относительно общей длины последовательности.
#  • Если последовательность пуста, возвращается 0.
# 3. Сортировка по GC-содержанию (Задание 2)

#  • Все последовательности из объединённого файла читаются и для каждой считается GC-содержание.
#  • Список кортежей (запись, GC%) сортируется по возрастанию GC-содержания.
#  • Для каждой последовательности выводится её ID, процент GC и сама последовательность.
# 4. Извлечение и аннотирование белковых последовательностей (Задание 3)

#  • Из файла GenBank извлекаются все признаки типа “CDS” (кодирующая последовательность).
#  • Для каждого такого признака выводится:
#  • ID белка
#  • Описание
#  • Координаты кодирующей последовательности (start, end, strand)
#  • Перевод (аминокислотная последовательность белка)

####Ошибки и их исправления

# Возможные ошибки и их решения:

#  1. Ошибка при отсутствии поля ‘translation’
#  • Если в feature отсутствует квалификатор ‘translation’, попытка обращения к `` вызовет ошибку.
#  • Решение: Используется метод .get('translation', ''), который возвращает пустую строку по умолчанию.

#  2. Некорректная работа с пустыми последовательностями
#  • В функции расчёта GC-содержания предусмотрена проверка на пустую последовательность (`if total_count == 0`), чтобы избежать деления на ноль.

#  3. Проблемы с кодировкой GenBank-файлов
#  • При открытии файлов для записи не указана кодировка, что может вызвать проблемы с нестандартными символами.
#  • Решение: При необходимости можно явно указать кодировку (`encoding="utf-8"`).

#  4. Дублирование записей при объединении
#  • Если в исходных файлах есть одинаковые записи, они будут продублированы в итоговом файле.
#  • Решение: Для устранения дубликатов можно использовать словарь по ID, но это не реализовано в текущем коде.

#  5. Потенциальные ошибки при работе с большими файлами
#  • При большом объёме данных возможно замедление работы из-за хранения всех записей в памяти.
#  • Решение: Для больших файлов рекомендуется использовать потоковую обработку.

#### Выводы

# В ходе выполнения лабораторной работы были освоены основные методы работы с биологическими последовательностями в формате GenBank с помощью Python и Biopython:
#  • Объединение данных: Научились объединять несколько файлов с последовательностями в один, что удобно для дальнейшего анализа больших наборов данных.
#  • Анализ нуклеотидных последовательностей: Реализовали функцию для расчёта GC-содержания, что является важным биоинформатическим показателем,
# влияющим на стабильность ДНК и экспрессию генов.
#  • Сортировка и визуализация данных: Научились сортировать последовательности по GC-содержанию и выводить их в удобном формате.
# • Извлечение белковых последовательностей: Получили практический навык аннотирования белков, извлечения их координат и аминокислотных
# последовательностей из геномных данных.
#  • Работа с аннотациями: Научились извлекать и интерпретировать биологическую информацию из сложных структурированных файлов.
# Данная работа заложила фундаментальные навыки для последующего анализа геномных данных, поиска генов, белков и их характеристик,
# а также для написания собственных скриптов по обработке биологических данных. Полученные знания и опыт пригодятся для решения
# более сложных задач в области биоинформатики и анализа данных о последовательностях ДНК и белков.
from Bio import SeqIO

file1 = "C:/Users/Professional/Downloads/sequence (1).gb"
file2 = "C:/Users/Professional/Downloads/sequence.gb"
output_file = "combined_species.gb"

with open(output_file, "w") as outfile:

    for record in SeqIO.parse(file1, "genbank"):
        SeqIO.write(record, outfile, "genbank")


    for record in SeqIO.parse(file2, "genbank"):
        SeqIO.write(record, outfile, "genbank")

print(f"Файлы успешно объединены в {output_file}.")


##########                ЗАДАНИЕ 2                ##############


from Bio import SeqIO

def calculate_gc_content(sequence):

    g_count = sequence.count('G')
    c_count = sequence.count('C')
    total_count = len(sequence)
    if total_count == 0:
        return 0
    return (g_count + c_count) / total_count * 100

def main(input_file):
    records_gc = []

    for record in SeqIO.parse(input_file, "genbank"):
        gc_content = calculate_gc_content(str(record.seq))
        records_gc.append((record, gc_content))

    records_gc.sort(key=lambda x: x[1])

    for record, gc_content in records_gc:
        print(f"ID: {record.id}, GC Content: {gc_content:.2f}%, Sequence: {record.seq}")

if __name__ == "__main__":
    input_file = "C:/Users/Professional/PycharmProjects/pythonProject/venv/combined_species.gb"  # Замените на путь к Вашему файлу
    main(input_file)


##########                ЗАДАНИЕ 3                ##############


from Bio import SeqIO


def extract_protein_sequences(input_file):
    for record in SeqIO.parse(input_file, "genbank"):


        for feature in record.features:
            if feature.type == "CDS":

                protein_id = record.id
                description = record.description
                location = feature.location
                translation = feature.qualifiers.get('translation', [''])[0]

                print(f"{protein_id}: {description}")
                print(f"Coding sequence location = [{location.start}:{location.end}]({location.strand})")
                print("Translation =")
                print(translation)
                print()


if __name__ == "__main__":
    input_file ="C:/Users/Professional/PycharmProjects/pythonProject/venv/combined_species.gb"
    extract_protein_sequences(input_file)